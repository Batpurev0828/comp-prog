#include <iostream>
#include <vector>
#include <algorithm>
typedef long long ll;
using namespace std;

void solve() {
    int n;
    cin >> n;
    vector<int> b(n);
    for (int &i : b) cin >> i;
    
}

int main(){
    cin.tie(0)->sync_with_stdio(0);
    int tc;
    cin >> tc;
    while (tc--) {
        solve();
    }
}